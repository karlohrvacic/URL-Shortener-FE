import { GetActiveElementsForUrlPipe } from './get-active-elements-for-url.pipe';

describe('GetActiveElementsForUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GetActiveElementsForUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
