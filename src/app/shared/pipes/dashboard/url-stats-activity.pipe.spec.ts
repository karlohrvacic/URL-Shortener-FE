import { UrlStatsActivityPipe } from './url-stats-activity.pipe';

describe('UrlStatsActivityPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlStatsActivityPipe();
    expect(pipe).toBeTruthy();
  });
});
