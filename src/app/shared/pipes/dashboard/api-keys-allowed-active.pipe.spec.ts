import { ApiKeysAllowedActivePipe } from './api-keys-allowed-active.pipe';

describe('ApiKeysAllowedActivePipe', () => {
  it('create an instance', () => {
    const pipe = new ApiKeysAllowedActivePipe();
    expect(pipe).toBeTruthy();
  });
});
