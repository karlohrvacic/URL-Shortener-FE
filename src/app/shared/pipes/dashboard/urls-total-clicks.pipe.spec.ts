import { UrlsTotalClicksPipe } from './urls-total-clicks.pipe';

describe('UrlsTotalClicksPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlsTotalClicksPipe();
    expect(pipe).toBeTruthy();
  });
});
